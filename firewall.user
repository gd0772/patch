# IPV6用户，请把 iptables 这两条规则前面加# 号，然后把 ip6tables 两条
# 规则前面的# 号去掉
# 如出现科学上网正常，国内访问慢的情况，请在 网络 接口 修改 物理设置，把 桥接口
# 模式 的打钩去掉，改为 以太网适配器: "eth0" (lan) 前面打钩，保存应用，并且把以
# 下带有 eth0 这条规则前面的# 号去掉 即可！如正常，可以无需理会保持默认关闭状态
# 以下规则只需把规则最前面的 # 号删除，然后 重启防火墙 立即生效，反之 默认不生效
